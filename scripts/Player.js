// highscore stuff
var SingleBeginner = new Array(10);
var SingleIntermedaite = new Array(10);
var SingleAdvanced = new Array(10);
var SingleExpert = new Array(10);

// player object
function NewPlayer(pos,nome,pontos,tempo){
    this.pos = pos;
    this.nome = nome;
    this.pontos = pontos;
    this.tempo = tempo;
};
// insert player in leaderboard
// insert a player in the singlebeginner array if it can be inserted
function insertPlayer_SingleBeginner(player){
    
}
